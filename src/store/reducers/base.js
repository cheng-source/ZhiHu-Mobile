const initial = {

}

const baseReducer = function(state = initial, action) {
    state = {...state };
    switch (action.type) {
        default: break;
    }
    return state;
}

export default baseReducer;