const initial = {

}

const storeReducer = function(state = initial, action) {
    state = {...state };

    switch (action.type) {
        default: break;
    }

    return state;
}

export default storeReducer;